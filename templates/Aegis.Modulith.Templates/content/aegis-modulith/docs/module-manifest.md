# Module Manifests

Each business module includes `module.json` with:

- `name`
- `schema`
- `type`
- `owner`
- `dependencies`
- `publicContracts`
- `features`
- `rules`

The manifest is metadata for humans, tools, and AI agents. Architecture tests still enforce boundaries.
