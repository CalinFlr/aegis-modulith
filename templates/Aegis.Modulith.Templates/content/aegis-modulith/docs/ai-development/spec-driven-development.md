# Spec-Driven Development

Enterprise AI output includes a `specs/` folder with a reusable spec template.

Use specs for meaningful changes that affect architecture, template UX, public API behavior, security posture, dependencies, CI, or generated workflow behavior.

Spec flow:

```text
spec -> plan -> tasks -> acceptance -> implementation -> validation
```
