# Agent Operating Model

`AGENTS.md` is the canonical instruction file for AI agents working in this generated repository.

Tool-specific files such as `CLAUDE.md` and `.github/copilot-instructions.md` only point to `AGENTS.md`. They should not duplicate architecture rules.

Use `OpenQuestions.md` for blockers, inferred decisions, and human-visible assumptions.
