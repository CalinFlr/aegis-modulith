# Skills

Generated skills mode: `AegisSkillsValue`.

- `none`: no `.agents/skills` output.
- `core`: skills for docs, module work, vertical slices, architecture review, module manifests, and specs.
- `enterprise`: the full enterprise skill set.

Each skill uses `SKILL.md` metadata and a validation section so agents can choose the relevant workflow without duplicating repository-wide rules.
