# Workflows

Enterprise AI output includes workflows under `.ai/workflows`.

Workflows describe repeatable steps for module creation, vertical slices, spec work, security fixes, and pre-PR review.

Use workflows with `AGENTS.md`, `OpenQuestions.md`, and the relevant skill instead of relying on prompt-only process memory.
